import styles from './Main.module.css';

const Main = () => {
   return (
      <section className={styles.container}>
         <h1 className={styles.main_title}>Main Page</h1>
      </section>
   );
};

export default Main;
